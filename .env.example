# Optional API endpoint for reservations module.
# If omitted, the app uses browser localStorage with seeded data.
VITE_RESERVATIONS_API_URL=http://localhost:3001

# Google Maps API key for Transfer map.
# Preferred variable name:
VITE_GOOGLE_MAPS_API_KEY=
# Backward-compatible alias:
VITE_GOOGLE_MAPS_KEY=
